global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: intellmeet-backend
    metrics_path: /metrics
    static_configs:
      - targets:
          - localhost:5000
          - backend:5000
